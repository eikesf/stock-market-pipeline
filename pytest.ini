[pytest]
# Filter out third-party deprecation and framework warnings from clean test reports
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
    ignore:.*copy keyword is deprecated.*
